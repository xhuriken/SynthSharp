<UserControl x:Class="SynthTest.Presentation.Views.Modules.MixerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SynthTest.Presentation.Views.Modules"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="250">
    <Border Background="#222" BorderBrush="#888" BorderThickness="2" CornerRadius="5" Padding="5" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Slider Grid.Column="0" Orientation="Vertical" Value="{Binding Vol1}" Maximum="1" HorizontalAlignment="Center"/>
                <Slider Grid.Column="1" Orientation="Vertical" Value="{Binding Vol2}" Maximum="1" HorizontalAlignment="Center"/>
                <Slider Grid.Column="2" Orientation="Vertical" Value="{Binding Vol3}" Maximum="1" HorizontalAlignment="Center"/>
                <Slider Grid.Column="3" Orientation="Vertical" Value="{Binding Vol4}" Maximum="1" HorizontalAlignment="Center"/>
            </Grid>

            <Grid Grid.Row="2" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ItemsControl Grid.Column="0" ItemsSource="{Binding Inputs}" ItemTemplate="{StaticResource InputPortTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <ItemsControl Grid.Column="1" ItemsSource="{Binding Outputs}" ItemTemplate="{StaticResource OutputPortTemplate}" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
        
    </Border>
</UserControl>
