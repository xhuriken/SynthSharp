<UserControl x:Class="SynthTest.Presentation.Views.Modules.AudioOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             Width="100" Height="120">
    <Border Background="#111" BorderBrush="#666" BorderThickness="2" CornerRadius="5" Padding="5" Margin="5">
        <StackPanel>
            <TextBlock Text="{Binding Name}" Foreground="#AAA" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Speakers" Foreground="Gray" HorizontalAlignment="Center" FontSize="10"/>

            <ItemsControl ItemsSource="{Binding Inputs}" ItemTemplate="{StaticResource InputPortTemplate}" HorizontalAlignment="Center" Margin="0,10,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>