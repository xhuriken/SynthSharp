<UserControl x:Class="SynthTest.Presentation.Views.Modules.AudioOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             Width="100" Height="200">
    <Border Background="#111" BorderBrush="#666" BorderThickness="2" CornerRadius="5" Padding="5" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Name}" Foreground="#AAA" FontWeight="Bold" HorizontalAlignment="Center" Height="15"/>

            <TextBlock Grid.Row="1" Text="Speakers" Foreground="Gray" HorizontalAlignment="Center" FontSize="10"/>
            <Slider Grid.Row="2" Orientation="Vertical" Value="{Binding Vol}" Maximum="0.5" HorizontalAlignment="Center" Height="100"/>

            <ItemsControl Grid.Row="3" ItemsSource="{Binding Inputs}" HorizontalAlignment="Center" Height="45">
                <!--<ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>-->
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>