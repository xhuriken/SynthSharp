<UserControl x:Class="SynthTest.Presentation.Views.Modules.ControlledAmplifierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             Width="100" Height="200">
    <Border Background="#111" BorderBrush="#666" BorderThickness="2" CornerRadius="5" Padding="5" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Name}" Foreground="#AAA" FontWeight="Bold" HorizontalAlignment="Center" Height="15"/>

            <Slider Grid.Row="1" Orientation="Vertical" Value="{Binding Level}" Maximum="1" HorizontalAlignment="Center" Height="80"/>

            <Grid Grid.Row="2" Height="45">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ItemsControl ItemsSource="{Binding Inputs}" HorizontalAlignment="Center" >
                    <!--<ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>-->
                </ItemsControl>
                <ItemsControl Grid.Column="1" ItemsSource="{Binding Outputs}" HorizontalAlignment="Center">
                    <!--<ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>-->
                </ItemsControl>
            </Grid>
        </Grid>
    </Border>
</UserControl>