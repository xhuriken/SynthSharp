<UserControl x:Class="SynthTest.Presentation.Views.Modules.AdsrView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SynthTest.Presentation.Views.Modules"
             mc:Ignorable="d" 
             Width="140" Height="250">

    <Border Background="#111" BorderBrush="#666" BorderThickness="2" CornerRadius="5" Padding="5" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Name}" Foreground="#AAA" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <Slider Orientation="Vertical" Height="80" Minimum="0" Maximum="5" Value="{Binding Attack}"/>
                    <TextBlock Text="A" Foreground="#FFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <Slider Orientation="Vertical" Height="80" Minimum="0" Maximum="5" Value="{Binding Decay}"/>
                    <TextBlock Text="D" Foreground="#FFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <Slider Orientation="Vertical" Height="80" Minimum="0" Maximum="1" Value="{Binding Sustain}"/>
                    <TextBlock Text="S" Foreground="#FFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <Slider Orientation="Vertical" Height="80" Minimum="0" Maximum="5" Value="{Binding Release}"/>
                    <TextBlock Text="R" Foreground="#FFF" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ToggleButton Grid.Column="0" 
                          IsChecked="{Binding GateTrigger, Mode=TwoWay}" 
                          Content="GATE" 
                          Margin="3,3,3,3" 
                          Padding="5" 
                          Background="#333" 
                          Foreground="White"/>

                <ItemsControl Grid.Column="1" ItemsSource="{Binding Outputs}" HorizontalAlignment="Center"/>
            </Grid>
            

        </Grid>
    </Border>
</UserControl>
